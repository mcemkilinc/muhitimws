extends ../layout

block content
  h4 Site Yönetim İşlemleri
  if user
   .well.well-sm
    | #{user.profile.name}
  ul.nav
   li(class=(title == 'Home') ? 'active' : undefined)
       a(href='/createMerchant') Esnaf Ekle
   li(class=(title == 'Home') ? 'active' : undefined)
       a(href='/manageMerchants') Esnaf Yönet
